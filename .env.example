# OpenCloudOS 9 服务器自动初始化配置
# 复制此文件为.env并根据实际情况修改配置

# 1. 主机名配置
# 设置服务器的主机名
HOSTNAME=server01

# 2. 网络配置
# 网络接口名称（如 eth0、ens33 等，留空则自动检测）
# NETWORK_INTERFACE=eth0
# 静态IP地址
IP_ADDRESS=192.168.1.100
# 子网掩码
NETMASK=255.255.255.0
# 网关地址
GATEWAY=192.168.1.1
# DNS服务器地址（多个地址用空格分隔）
DNS_SERVERS=8.8.8.8 8.8.4.4

# 3. SSH配置
# SSH服务端口（建议修改为非默认端口，增强安全性）
SSH_PORT=2222
# SSH登录用户名（脚本会为该用户生成密钥对）
SSH_USER=admin
# 是否启用密码验证（yes/no，建议设置为no，仅使用密钥登录）
PASSWORD_AUTHENTICATION=no

# 4. 安全配置
# 是否启用防火墙（yes/no）
ENABLE_FIREWALL=yes
# 是否安装并启用Fail2ban（yes/no，用于防止SSH暴力破解）
INSTALL_FAIL2BAN=yes
